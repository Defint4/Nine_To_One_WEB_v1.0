# Adresse publique de l'API, embarquée dans le build (REST et WebSocket passent par nginx
# sur le même domaine). Changer cette valeur impose un `pnpm build`.
NEXT_PUBLIC_API_URL=https://games.matthieuguiot.dev
